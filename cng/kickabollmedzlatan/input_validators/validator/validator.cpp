#include <bits/stdc++.h>
#include "validator.h"


using namespace std;


void run() {
	int x = Int(2, 10);
	Endl();
	Int(0, 1000);
	Endl();

	for (int i = 0; i < x-1; i++)
	{
		Int(0, 2000);
		Endl();
	}
	Eof();
}
