#include <bits/stdc++.h>
#include "validator.h"


using namespace std;


void run() {
	int n = Int(1, 10);
	Endl();

	for (int i = 0; i < n; i++)
	{
		Int(1, 10);
		Space();
		Int(1, 10);
		Endl();
	}
	Eof();
}
